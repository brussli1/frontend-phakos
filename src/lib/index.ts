// place files you want to import through the `$lib` alias in this folder.
// Assuming this is the structure of your category data
interface Category {
    attributes: {
      slug: string;
      categoria: string;
    };
  }
  
  let categoria: Category[] = [];
  